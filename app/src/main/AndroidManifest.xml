<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leaderium.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthActivity" />
        <activity android:name=".ProektoriaCasesActivity" />
        <activity android:name=".EventsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EventsDetailActivity" />
        <activity android:name=".CoursesActivity" />
        <activity android:name=".CoursesDetailActivity" />
        <activity android:name=".proektoria_cases" />
        <activity android:name=".ProektoriaCompaniesActivity" />
        <activity android:name=".full_description" />
        <activity android:name=".BotAshot"></activity>
    </application>

</manifest>